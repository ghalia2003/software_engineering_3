import 'package:flutter/material.dart';

class ColorsTheme {
  // 🔵 اللون الأساسي للتطبيق (AppBar, Buttons, Primary Actions)
  static const Color primaryBlue = Color(0xFF0D47A1);

  // 🔷 أزرق ثانوي ناعم (للأيقونات، Highlight، Secondary Buttons)
  static const Color secondaryBlue = Color(0xFF1976D2);

  // 🤍 الخلفية الرئيسية للتطبيق
  static const Color backgroundWhite = Color(0xFFF9FAFB);

  // 🧾 خلفية الكروت (Cards, Containers)
  static const Color cardWhite = Color(0xFFFFFFFF);

  // 🖤 لون النص الأساسي (العناوين، الأرقام المهمة)
  static const Color textDark = Color(0xFF1F2933);

  // 🌫️ لون النص الثانوي (وصف، ملاحظات، تفاصيل خفيفة)
  static const Color textGrey = Color(0xFF6B7280);

  // ➖ لون الحدود والفواصل (Borders, Dividers)
  static const Color borderGrey = Color(0xFFE5E7EB);

  // ✅ لون النجاح (عمليات ناجحة، تحويل تم)
  static const Color successGreen = Color(0xFF16A34A);

  // ⚠️ لون التحذير (تنبيه، رصيد منخفض)
  static const Color warningOrange = Color(0xFFF59E0B);

  // ❌ لون الخطأ (تحويل فشل، كلمة مرور خاطئة)
  static const Color errorRed = Color(0xFFDC2626);
}
